# dns-lookup
DNS lookup with multiple record types. Zero dependencies.
## Usage
```
python3 dns_lookup.py google.com
python3 dns_lookup.py example.com MX TXT NS
python3 dns_lookup.py --reverse 8.8.8.8
```
